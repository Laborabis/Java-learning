package Window123;

import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.layout.StackPane;
import javafx.stage.Stage;

/**
 * Created by Adam on sobota, 2/lis/2019
 */
public class Window123 extends Application {
    public static void main(String[] args) {
        launch(args);
    }

    @Override
    public void start(Stage primaryStage) throws Exception {

        StackPane stackpane = new StackPane();
        Button button = new Button("My first button");
        stackpane.getChildren().add(button);

        Scene scene = new Scene(stackpane, 400, 600);

        primaryStage.setScene(scene);
        primaryStage.setTitle("My First FX Window 123");
        primaryStage.show();
    }
}
